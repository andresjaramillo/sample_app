<h1>Edition du profil</h1>

<%= form_for(@user) do |f| %>
	<%= render 'fields', :f => f %>
	
	<div class="actions">
		<%=f.submit "Update"%>
	</div>
	
	<div>
  <%= gravatar_for @user %>
  <a href="http://gravatar.com/emails" target="_blank">changer</a>
</div>
<% end %>
